[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip uninstall -y pyswisseph || true",
  "pip install -r requirements.txt"
]

[start]
cmd = "python monitor_loop.py"
