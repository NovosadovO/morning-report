# Кнопки під усіма сповіщеннями (запит Олега 04.08)

## Що робимо
1. Пояснити, що робить 📝 Нотатка (зараз зберігає автотекст, не питає введення).
2. Нотатка → питає ТВІЙ текст (force_reply + state awaiting_note).
3. Нові кнопки під календарними нагадуваннями (cw_*).
4. Універсальні кнопки під УСІМА темами (новий модуль ai_buttons.py, префікс gx_).

## Прогрес
- [x] ai_buttons.py створено (keyboard/detect_topic/do_more/do_note/do_later/do_mute/do_done/tick/pending/report/mute_status)
- [x] calendar_watch: нові кнопки + do_go/do_map/do_focus/do_run/do_next/do_day/do_refresh/do_bills/ask_note_text
- [x] bot.py: хендлери gx_*, cw_*, state awaiting_note, команди /кнопки /відкладені /приховані_теми /увімкни_теми
- [x] message_generator._send_to_telegram(text, topic, trigger_type) + mute-фільтр
- [x] intelligent_listener: ai_buttons.tick() + gc
- [x] тести (tests_ai_buttons.py, 0 помилок) + лінт bad:0 + деплой 9e7b69f2 SUCCESS

## Нотатки
- planner.set_state/get_state/clear_state; невідомі mode планер ігнорує → безпечно.
- charts.plot_crypto_trend / plot_health_2x2_dashboard / plot_habits_heatmap; strava_charts.plot_month_chart
- deadlines_watcher.upcoming(days), bills_watcher.monthly_report()
- лінт: python3 /tmp/lint311.py <files> → bad: 0

## feedback_ctx (2026-08-04, деплой 120c4cf7 SUCCESS)
- НОВИЙ feedback_ctx.py: build(days)/stats/muted_topics — агрегує gx_ack + calendar_ack + response_log + ai_notes у блок "📌 ЗВОРОТНИЙ ЗВ'ЯЗОК ОЛЕГА" для промптів. Кеш 180с, ніколи не падає, порожньо → "".
- Підключено: context.py (чат), message_generator.py (проактивні, +правило 8), calendar_watch._ai_note/_ai_digest через _fb_block().
- ai_buttons._ack + calendar_watch._ack тепер пишуть у response_log (категорії ai_button / calendar_button) → єдиний лог.
- bot.py: команда /пам_ять_аі (/ai_memory, /що_знає_аі) — показує сирий блок + статистику. Webhook 200, "Message: /пам_ять_аі" у логах.
- tests_feedback_ctx.py (=/tmp/fb_test.py): 15 перевірок, ❌=0. Решта 4 набори теж ❌=0. lint311 bad:0.

## Верифікація 04.08 (деплой b6d2664a SUCCESS, 0 помилок у логах)
Перевірено НЕ тестами, а реальними callback через webhook. Знайдено і виправлено 4 справжні баги:
1. `_ask_note` → NameError: TELEGRAM_CHAT_ID (константа зветься TELEGRAM_CHAT) — кнопка нотатки НЕ питала текст, тихо падала у fallback. Тепер приймає chat_id.
2. Автонотатка-fallback зберігала все тіло сповіщення ("Привіт Олеже! 👋...") → нове _auto_note(): без вітання, [тема] + 160 симв.
3. calendar_watch.do_note писав "Подія: None (None)" при payload без title.
4. feedback_ctx тягнув це сміття в промпт → _is_junk() + _note_of() + _useful_resp() (сирі callback_data з response_log відсіяно).
Почищено прод: ai_notes 9 → 4 записи (видалено 5 сміттєвих).
E2E підтверджено в логах: [CB] gx_note_ → force-reply без помилки → "перевірка нотатки: тримати BTC до 120k" → [ai_notes] added (gx_email).
Тести: fb_test, note_test, cw_test, cw_week_test, cw_ai_test, cw_month_test — усі ❌=0. lint311 bad:0.
Нове в репо: tests_feedback_ctx.py, tests_notes_regression.py.

## Confirm-шар (двокрокове підтвердження) — 08.08.2026
- `confirm.py`: register/ask/yes/no/report/gc, TTL 24 год, префікси `cfm_y_`/`cfm_n_`, store `confirm_store.json`, лог `confirm_log.json`.
- Деструктивні кнопки `🚫 Скасовано` (`cw_cancel_`) і `🔇 Не нагадувати` (`gx_mute_`) НЕ виконують дію одразу — лише питають.
- Постійний блок нагадувань: `calendar_blocked.json` (НЕ чиститься `gc_sent`), `tick()` і `_fire_snoozed()` його поважають.
- Команди: `/вимкнені_нагадування`, `/увімкни_нагадування`, `/підтвердження`.
- Перевірено в прод (deploy f6476bba, SUCCESS): cw_cancel → питання → cfm_n (нічого) → cfm_y → запис у calendar_blocked; gx_mute → cfm_y → запис у gx_mute; `/увімкни_нагадування` → "повернуто нагадувань: 1". Traceback/NameError у логах немає.
- Тести: tests_confirm.py (34 перевірки, ❌=0) + fb/note/cw/cw_week/cw_ai/cw_month — усі 0. Лінт 3.11 bad: 0.
- ⚠️ Відкрито: Strava API 403 Forbidden на всіх запитах (get_activities / get_last_activity / get_week_stats) — потрібен новий refresh_token або перевірка підписки.

## Оновлення 09.08.2026
- Підтвердження додано ще під 2 кнопки: `❌ Не було` (cw_miss) і `🔔 Нагадай пізніше` (gx_later). Перевірено в проді: питання → cfm_y → запис `missed` у calendar_ack; cfm_n → нічого.
- `confirm.register()` має поле `revert` — після «Так» бот пише, як повернути назад.
- Огляд «місяць вперед» тепер ЩОТИЖНЯ: понеділок 12:00–17:00 (`_monthly_tag()` = рік+ISO-тиждень). Огляд тижня лишився пн 06:00–11:00.
- Strava 403: причина НЕ токен, а `Application/Inactive` (застосунок 228739 деактивовано Strava). Додано `api_blocked()`, `app_inactive_reason()`, `_note_api_error()` — пауза 30 хв замість спаму, зрозумілий рядок у /diag, дані з кешу.

## storage merge-on-conflict — 10.08.2026
- КОРІНЬ «мертвих кнопок»: `_save_github` при 409 (інший потік записав файл між нашим GET і PUT) перезаписував файл своїм знімком → ключі іншого потоку зникали (втрачені відповіді на кнопки, драфти, підтвердження).
- FIX: на повторній спробі беремо свіжий remote як базу і накладаємо свої ключі поверх (`merged = remote | data`). Для не-dict (списки) поведінка без змін. Лог 409-retry притишений (пише лише з 3-ї спроби).
- Тест `tests_storage_merge.py` (фейковий GitHub, 409 на першому PUT): 6 перевірок, ❌=0 — чужий ключ не втрачається, свій записується, список зберігається.
- Деплой `01558676` SUCCESS: 0 Traceback, 0 GAVE UP, 0×409 у логах.
- `email_cancel_` тепер видаляє драфт зі сховища + пише «нічого не надіслано» (перевірено в проді: запис зник).

## Універсальний confirm-гейт (15.08)
Вимога Олега: «після КОЖНОГО нагадування та сповіщення» — кнопки на кшталт
«не потрібно нагадувати» / «записувати» спершу перепитують.

- `_CONFIRM_GATE` у bot.py: 32 префікси (skip-и, записи в календар/список,
  «оплачено/зроблено», надсилання листів) → своє питання/кнопки/текст-після/як повернути.
- `_CONFIRM_GATE_SKIP`: cw_cancel_, cw_miss_, gx_mute_, gx_later_, email_delete_,
  calrem_skip_, cfm_ — вони перепитують у власних обробниках (щоб не питати двічі).
- `_confirm_gate()` стоїть на вході `_route_callback(cb, confirmed=False)` — one place,
  накриває всі модулі. Помилка гейту ніколи не ламає кнопку (fallback = стара поведінка).
- Тема питання (`_gate_subject`) — текст самої натиснутої кнопки, потім 1-й рядок повідомлення.
- Після «Так» дія не дублюється в confirm-хендлері: `_cfm_h_gate` повертає
  `{"redispatch": <оригінальний callback_data>}`, а cfm_y_ гілка викликає
  `_route_callback(cb2, confirmed=True)`.
- Тест: `tests_confirm_gate.py` (AST-перевірка префіксів, конфліктів зі SKIP,
  longest-prefix match, повнота текстів) → fails: 0.
- Прод (деплой 0cf3b196 SUCCESS): gate_cal_skip ask→yes→«gate: виконую»,
  gate_calrem_add ask→no→«скасовано». 0 Traceback.

## Режим сну /сон (17.08)
Вимога: «/сон → без сповіщень, без нагадувань, без витрат кредитів AI; з 04:00 бот сам відновлює роботу».

- `quiet.py`: стан у `quiet_mode.json` (storage → гілка data, переживає редеплой) {until, since, wake_hour}. `sleep_on()` пише until = найближчі 04:00.
- АВТО-ПРОБУДЖЕННЯ БЕЗ КРОНУ: `is_quiet()` порівнює now з until; коли дедлайн сплив — `_auto_wake()` чистить стан. Немає воркера, який міг би не запуститись.
- `blocked(kind)` — єдина перевірка. `mark_user_thread()` (threading.local) у `handle_command` і `_route_callback` → команди/кнопки Олега працюють і вночі.
- quiet-guard вставлено у 22 файли / ~30 функцій-сендерів. Маркер у коді: `quiet-guard`.
- AI-кредити: guard у `monitor._gem_post` (усі модулі ходять через нього) + `ai_notes.py` (єдиний, що бив у Gemini напряму).
- Команди: `/сон` (=/добраніч,/спати), `/прокинувся` (=/підйом), `/режим_сну`. Аналіз сну переїхав з `/сон` на `/аналіз_сну`; /help оновлено.
- Тести: `tests_quiet.py` — 40 асертів → fails: 0.

## Режим сну — ФІКС ДІРОК (18.08)
Олег: «ввів команду, а сповіщення далі приходили».
Причини (дві реальні, обидві закриті):
1. 8 фонових функцій шлють sendMessage НАПРЯМУ, минаючи покриті сендери:
   monitor.check_smart_notifications / check_event_done / check_sleep_quality /
   check_mood_evening, monitor_loop.run_astro_alert_watcher, proactive._do_request,
   habits.api, meds.api, weekly_report.api → guard додано в кожну.
2. Стан читався через storage (GitHub) з CACHE_TTL=300s → фонові чекери й
   subprocess-и (report2/report_defi/report_social) до 5 хв не бачили /сон.
   Тепер quiet._load читає /tmp/quiet_mode.json (спільний для всіх процесів
   контейнера) + memcache 10 c; GitHub лишився лише для переживання редеплою.
3. Плюс: /звіт та інші команди виконуються у фоновому потоці, куди
   threading.local не наслідується → його ж звіт було б заглушено. Додано
   вікно активності (`touch_user`, ACTIVE_MIN=5): Олег писав <5 хв тому → нічого
   не глушимо. sleep_on() скидає це вікно в 0, щоб тиша починалась НЕГАЙНО.
Тест: у tests_quiet.py додано anti-leak перевірку — обхід усіх *.py, будь-яка
функція з api.telegram.org без quiet-guard = ❌ (whitelist лише реакції на дії
Олега: bot.api/handle_command/_download_telegram_file/handle_health_zip,
health_ocr.download_telegram_photo). fails: 0.
Прод-перевірка (деплой fcf2d2f2): /сон → 6 хв тиші → у логах 0 надісланих
повідомлень, натомість `[quiet] 🌙 сон: check_* пропущено` × багато і
`режим сну — AI-виклик пропущено` для action_detect/proactive_ai.
Станом на кінець: режим сну ВИКЛЮЧЕНО на прохання Олега (until: null).

## Єдиний мозок AI — ai_brain.py (20.08)
- Коміти: 6269501ae22 (ai_brain + /мозок + tests_ai_brain.py), + фікс UnboundLocalError. Деплої a86aadb3 і 0733467b — SUCCESS.
- ai_brain.wrap() інжектиться ЦЕНТРАЛЬНО в monitor._gem_post → пам'ять+свобода в КОЖЕН AI-виклик (раніше feedback_ctx був лише в 3 з ~24 промптів).
- Прод-лог підтвердив: [ai_brain] +1670..+3176 симв. пам'яті/свободи → action_detect / context_ask_ai / email_ai_item.
- Пам'ять: ai_answers.json (кожне текстове повідомлення Олега, щоденник, мікро-опитування), ai_last_topics.json (про що AI вже писав, 36 год → не повторюватись; буфер, запис у storage не частіше 240 с).
- JSON-промпти НЕ отримують блок свободи (is_json_prompt) — парсери не ламаються.
- Команда /мозок (мозок, /пам_ять_аі) — показує все, що AI пам'ятає.
- ФІКС: у handle_command блоки щоденника і мікро-опитування падали з UnboundLocalError (datetime/os шадовились локальними import) — відповіді НЕ зберігались взагалі. Тепер аліаси _dtc_d/_os_d/_dtc_mc2 + запис у пам'ять AI.
- Тести: tests_ai_brain.py fails: 0 (44 асерти). Регресія: gate_test 0, email_btn_test 0, tests_quiet 0, confirm_test 0, cw_test 0.
- Gemini білінг ЖИВИЙ (0 "billing depleted" у логах, реальні AI-тексти). Strava 228739 досі Inactive.

## Постійний блок-лист «не нагадувати» (20.08)
Проблема: підтверджене «не треба» ніде не зберігалось (cal_skip_/calrem_skip_/email_keep_
лише прибирали клавіатуру) → та сама подія/лист приходили знову.
Рішення: dismissed.py — блок-лист у data/dismissed.json, матчинг ЗА КЛЮЧЕМ (kind|id)
І ЗА НОРМАЛІЗОВАНОЮ НАЗВОЮ (без емодзі/регістру/Re:), бо id пропозицій щоразу нові.
Хук один: confirm.yes() → dismissed.remember_confirm() для MUTE_ACTIONS (15 gate_* + cw_cancel,
cw_miss, calrem_skip, gx_mute, email_delete). Текст кнопки («Не треба») назвою НЕ стає —
_BAD_TITLES + fallback на тему з тексту повідомлення (extra.msg із гейту).
Перевірки перед відправкою: monitor.apply_action_suggestion, monitor.check_important_emails_followup
(×2 копії), calendar_watch.is_blocked(evid, title), followup_watcher.check.
email_keep_ → _drop_important_email() прибирає лист з important_emails.json + mute.
/вимкнені_нагадування показує і dismissed; /увімкни_нагадування чистить обидва.
Тести: tests_dismissed.py → fails: 0. Прод: деплой 39322aa7 SUCCESS, живий клік
cal_skip → cfm_y_ → запис у data/dismissed.json (ключ+назва), 0 Traceback.

## Розуміння стану справ — lifecycle.py (20.08)
Проблема: бот нагадував про те, що вже зроблено. Приклади Олега: «поїздка на Корфу
вже пройшла — сьогодні прилетів», «страховку оплатив — не нагадуй, а напиши з якого
по яке число діє і запиши наступну оплату в календар».
Рішення: lifecycle.py — реєстр справ у data/lifecycle.json.
- Промпт monitor._gemini_email_analysis розширений полями: state (paid/completed/
  pending/unknown), entity, entity_kind (insurance/subscription/booking/trip/bill/doc),
  valid_from, valid_to, next_due, keyword. ОКРЕМОГО Gemini-виклику НЕМА (економія білінгу).
- Жорстке правило в промпті: «ЖОДНИХ ВИГАДАНИХ ДАТ І СУМ. Чого немає в листі — null».
  Немає дат → пропозиції про календар немає взагалі.
- state=paid/completed → lifecycle.remember() закриває тему: _close_reminders()
  глушить нагадування через dismissed (keyword-матчинг з урахуванням укр. відмінків,
  _stem: ≥7 симв → -2, ≥5 → -1; потрібні ВСІ слова keyword-а, keyword ≥4 симв).
  Далі apply_action_suggestion для цього листа не викликається (if not _closed).
- Замість нагадування — картка «✅ Бачу — вже оплачено» з періодом дії і кнопкою
  «📅 Записати все» (lc_add_) / «Не треба» (lc_skip_). Кнопки під confirm-гейтом
  (gate_lc_add), payload через ai_kit.PayloadStore → мертвих кнопок нема.
- next_due виводиться як valid_to + 1 день лише для insurance/subscription/booking.
  Нагадування про оплату ставиться за RENEW_LEAD_DAYS=14 до next_due (якщо дата вже
  минула — на сам next_due). check_expiring() у monitor_loop.run_assistant_watcher.
- Захист від поспішного AI: state=completed з датою в майбутньому тему НЕ закриває.
- Команди: /справи (/статус_справ, /lifecycle) — реєстр; /перевір_справи
  (/check_lifecycle) — ручна перевірка термінів.
- ФІКС (критичний): apply_action_suggestion(...) у check_new_emails стояв УСЕРЕДИНІ
  except Exception: pass блоку self_diagnostics → проактивні пропозиції з листів не
  працювали взагалі (лише при падінні імпорту, і з даними ОСТАННЬОГО листа).
  Перенесено в тіло циклу for; позиція перевіряється AST-тестом у tests_lifecycle.py.
- Тести: tests_lifecycle.py → fails: 0 (7 блоків: Корфу, страховка, без дат, поспішний
  AI, попередження, /справи, інтеграція+AST). Регресія: tests_dismissed 0, tests_quiet 0,
  gate_test 0, email_btn_test 0, cw_test 0, confirm_test 0, dm_test 0.
- Прод: коміт c6a52930fe7, деплой cb8e6f11 SUCCESS. /справи і /перевір_справи → 200,
  0 Traceback / 0 NameError / 0 ImportError. Маршрут кнопки перевірено живим кліком:
  lc_add_ → gate_lc_add → cfm_y_ → «виконую lc_add».
- Дані: lifecycle.json, lifecycle_sent.json, lifecycle_store.json (гілка data).
- ЗАЛЕЖНІСТЬ: реєстр наповнюється лише коли Gemini живий — без AI поля state/valid_to/
  next_due не заповнюються і lifecycle нічого не бачить.

## Пропозиції не повторюються (20.08)
Скарга: «дав команду не нагадувати про лист від Вероніки, підтвердив страховку — приходить знову».
Три причини і фікси:
1. Блок-лист матчив лише точну назву — AI щоразу формулює інакше.
   → `dismissed._fuzzy()`: спільні корені слів, ≥2 і ≥75% коротшої назви.
     «Відповісти на лист від Вероні» == «...від Вероні щодо весілля»,
     але «страховка Kooperativa» != «страховка квартири».
2. Не було журналу вже показаних пропозицій (id щоразу новий).
   → `offer_log.json`: `mark_offered()` при відправці (7 днів) і при будь-якому
     натисканні кнопки (60 днів, decided); `already_offered()` перед відправкою.
3. Пропонувались дати в МИНУЛОМУ (страховка «до 15.08», коли вже 20.08).
   → фільтр у `apply_action_suggestion`.
Плюс `bot._close_offer()` — кожне натискання (cal_add/calrem_add/shop_add/
cal_skip/calrem_skip/shop_skip) закриває пропозицію; «не треба» ще й у блок-лист.
Add-кнопки НЕ mute-ляться (щоб calendar_watch міг нагадати про саму подію).
Тест: `tests_no_repeat.py` (fails: 0). Деплой b0e9863c SUCCESS.
Вручну закрито в data/dismissed.json: «Вероні/весілля», «страховка Kooperativa».

## Інтернет для AI + розумна тиша (20.08)
**grounding.py** — Gemini отримує google_search для обраних тегів (crypto_ai,
personal_ai, themes_ai, daily_rec, deep_analysis, briefing, proactive_ai,
context_ask_ai, weekly/monthly_coach, defi_digest). Раніше AI знав лише те, що
дають наші API, і спирався на знання 2024 р.
Захисти: JSON-промпти НЕ грунтуються (парсер зламався б); при 400/403 з tools
`strip()` знімає їх і виклик повторюється без інтернету; маркери `[cite: ...]`
чистяться з тексту; `footer()` дає рядок «🌐 Джерела: ...».
Вплетено в `monitor._gem_post` ПІСЛЯ ai_brain-інжекту.

**autoquiet.py** — тиша за реальним графіком змін (не вручну /сон):
нічна зміна → спить 07:00–14:00; рання → 22:00–05:00; вихідний → 23:00–07:00.
На зміні НЕ вважається сплячим (стара скарга «пише що я сплю, а я на нічній»).
Джерело: context.get_shift_from_calendar(); без календаря fallback 23:00–07:00.
Несрочне кладеться в чергу `held_msgs.json` і після пробудження приходить
дайджестом «☀️ Поки ти спав»; повідомлення з кнопками віддаються окремо, щоб
кнопки лишились робочими. Термінове (VIP/крипто-алерт/подія за годину/аварія)
проходить одразу — URGENT_HINTS. Черга старша за 10 год не шлеться.
Точки входу: ai_kit.tg, monitor._send_telegram_chunk,
monitor._send_telegram_text_with_keyboard; flush у run_assistant_watcher.
Команди: /тиша, /покажи_відкладене.
Тест: tests_smart.py (fails: 0).

## Тиша тільки вручну (20.08)
Олег: «Режим тиші має працювати ТІЛЬКИ коли я введу /тиша, і бот о 04:00 сам відновиться».
- autoquiet.py: викинуто автоматичне визначення сну за графіком змін. state()/sleeping() тепер = quiet.is_quiet() (ручний режим, until = найближчі 04:00, авто-пробудження при спливанні дедлайну). should_hold() під час тиші відкладає ВСЕ (навіть термінове) — тиша це тиша, нічого не губиться.
- Порядок у відправниках змінено: autoquiet-черга ПЕРЕД quiet-guard (ai_kit.tg, monitor._send_telegram_chunk, monitor._send_telegram_text_with_keyboard) — раніше guard викидав повідомлення, тепер воно чекає до 04:00.
- bot.py: /тиша (+/розумна_тиша) = quiet.sleep_on() з карткою «РЕЖИМ ТИШІ УВІМКНЕНО … до 04:00». Статус переїхав на /тиша_статус (+/quiet_status). /help оновлено.
- Флаш черги: monitor_loop.run_assistant_watcher (not sleeping() and pending() -> flush()).
- tests_smart.py розділ 2 переписано під ручний режим: fails: 0. Лінт 3.11 bad: 0, AST ok. Решта тестів 0.
- Коміт b5af9e1479d, деплой 29edc532-8c4c-4834-b403-b81e19425044 SUCCESS (Traceback/NameError/ImportError = 0). /тиша_статус у проді → HTTP 200, у логах "Message: /тиша_статус" без помилок.

## Питання «Точно?» і видалення — під своїм сповіщенням (20.08)
Олег: «Запитування Точно і видаляння потрібно поставити під тим сповіщенням, до якого стосується».
- bot.py: нові `_btn_row()`, `_ask_confirm_inline(cb,q)`, `_cfm_origin(r)`, `_cfm_close_inline(cb,r,status,keep_kb)`.
- Гейт `_confirm_gate` більше НЕ шле нове повідомлення: редагує reply_markup ТОГО САМОГО сповіщення → рядок-підпис «⚠️ Точно? <тема>» (callback noop) + [Так][Ні]. Fallback, якщо edit не вдався: sendMessage з reply_to_message_id (не в кінець чату), далі старий send_with_keyboard.
- confirm.py: `ask()` повертає subject/question; новий `attach_origin(cid, chat_id, msg_id, orig_kb)` — запам'ятовує, під яким сповіщенням стоїть питання і які кнопки там були; `yes()`/`no()` повертають `extra`.
- «Ні» → повертає початкові кнопки на місце + підпис «👍 Залишив як було», без нового повідомлення. «Так» → підпис-результат (done_text) замість кнопок; окреме повідомлення тільки якщо є хвіст (час/як повернути) і теж reply на те сповіщення.
- Інлайн також для гілок, що питають самі: `email_delete_` (видалення листа) і `calrem_skip_`, `cw_miss_`.
- tests_inline_confirm.py — НОВИЙ, fails: 0. Решта тестів 0, лінт 3.11 bad: 0, AST ok.

## ФІКС: пошук з'їдав вивід — повідомлення приходили обрізані (20.08)
Симптом у проді: `[grounding] 🌐 інтернет увімкнено для MSG_DEEP_ANALYSIS`, але далі `Sending 213 chars` / `67 chars` замість 500-650 слів.
Причина (перевірено живим викликом Gemini): inject просто ЗНІМАВ `thinkingBudget:0`, модель отримувала необмежене думання і витрачала ~1758 токенів із maxOutputTokens=1400 → на текст лишалось 180 токенів = 156 символів, finishReason MAX_TOKENS.
Заміри: без tools — 4240 симв; tools + вільне думання — 156 симв (thoughts 1758, out 180); tools + thinkingBudget 512 + out 3000 — 6484 симв, 5 джерел.
Фікс у grounding.inject: `THINK_BUDGET = 512` (не 0 — 0 ріже пошук, не безліміт — з'їдає вивід), `MIN_OUT_TOKENS = 3000`, maxOutputTokens = max(поточний, 3000) — більшу стелю не занижуємо.
tests_smart.py: 2 старі тести перевіряли попередню (хибну) поведінку — оновлені; +блок «Пошук не має з'їдати вивід». fails: 0.

## ФІКС: повідомлення обривались на півслові (20.08)
Скарга: «…використовувати його максимально ефективно, ад» (скрін IMG_1900).
Причина: Gemini впирався в maxOutputTokens (finishReason=MAX_TOKENS) — текст
приходив рівно до ліміту. Ліміти розкидані по 40+ місцях (300…8000).
Рішення — централізовано в monitor._gem_post:
1. notrunc.bump() — один повтор із ×2 стелею (CAP 8000) + стеля думанню 512;
2. notrunc.tidy() — якщо й тоді обрив: дорізаємо обірване слово і «висячі»
   службові слова/коми, завершуємо крапкою; якщо хвіст короткий — ріжемо до
   останнього цілого речення. JSON-промпти через tidy не проходять.
3. message_generator: max_tokens 350/700/1400 → 900/1800/3500 (українська
   кирилиця ≈2.5-3 токени/слово, 650 слів ≈1800 токенів).
4. _chunk_text ріже по пробілу, не посеред слова.
Тести: tests_notrunc.py (33 перевірки) — fails: 0.

## ФІКС: обрізані повідомлення на півслові (21.08)
Скарга: повідомлення обривалось посеред слова («…використовувати його максимально ефективно, ад»).
Причина: ліміти maxOutputTokens розкидані по 40+ місцях (300..8000), а українська кирилиця
токенізується дорого (~2.5-3 токени/слово) — 650 слів ≈ 1800 токенів, ліміт 1400 різав текст.
Рішення (централізовано в monitor._gem_post):
- новий модуль notrunc.py: truncated() (finishReason=MAX_TOKENS), bump() (×2, CAP=8000, один раз
  за виклик), tidy() (обрізає до останнього цілого речення, MIN_KEEP=0.55), fix_response().
- порядок: спершу добираємо токени і повторюємо запит, і лише якщо не допомогло — чистимо хвіст.
- JSON-промпти через tidy не проходять (парсер зламався б).
- message_generator: max_tokens 350→900, 700→1800, 1400→3500; _chunk_text ріже по пробілу.
Тести: tests_notrunc.py (9 блоків) — fails 0. Регресія 10 наборів — 0.

## 3 НОВІ ТРЕКЕРИ (21.08): дати, підписки, RWA
Олег вибрав зі списку рекомендацій: (9) дні народження й важливі дати, (4) регулярні
платежі та підписки з пошти, (3) RWA топ-10. Подача: важливе — окремим сповіщенням,
решта — у щоденному звіті.

### dates_book.py (🎂 важливі дати)
- файли: dates.json (реєстр), dates_store.json (payload кнопок), dates_sent.json (дедуп).
- рік не обов'язковий — дата повторюється щороку; 02-29 у не-високосний рік → 02-28.
- пороги нагадувань: 7 / 3 / 1 / 0 днів до дати, кожен поріг раз на рік (дедуп по ключу).
- parse_date розуміє 14.03, 12.03.1990, 1990-03-12, «2 листопада».
- команди: /дати (список), /дата <Ім'я> <дата> (додати), /дати_імпорт (з Google Calendar).
- кнопки: db_wish_ (AI-привітання, тег MSG_WISH), db_gift_ (ідеї подарунка, MSG_GIFT),
  db_cal_ (створює 3 події — на 3 роки вперед), db_snooze_, db_skip_.
- контекст для привітання беремо з people_memory (people.json), щоб текст був живий.

### subs_watcher.py (🔄 підписки й регулярні платежі)
- файли: subs.json, subs_store.json, subs_sent.json, subs_scan.json, subs_due_sent.json.
- джерело — пошта (IMAP). Підписка вважається підтвердженою, коли AI прямо бачить
  регулярність АБО є ≥2 списання в різні місяці (_distinct_months) — інакше не чіпаємо.
- _add_months зберігає число місяця: 14.08→14.09, 31.01→28.02, +12м→наступний рік.
- річна підписка ділиться на 12 для місячної суми (monthly_total).
- попередження за 3 дні ДО списання (DUE_WARN_DAYS=3), подорожчання ловиться від +3%.
- максимум 3 картки за скан, скан не частіше ніж раз на 180 хв.
- кнопки: sb_ok_, sb_rem_, sb_cancel_ (інструкція як скасувати + економія), sb_stop_, sb_skip_.

### rwa_radar.py (🏦 RWA топ-10)
- джерела: CoinGecko coins/markets?category=real-world-assets-rwa, coins/categories,
  DeFiLlama /protocols (обов'язково з -L, інакше 301).
- ВАЖЛИВО: токенізовані фонди (USDY/USYC/BUIDL/EURSAFO) — не монети. Для них ±8% це не
  «ріст», а аномалія, тому пороги різні: монета ±8%, ONDO (моя) ±5%, сектор ±5%,
  фонд ch24 <= -1.0% або >= +2.5%. Нова монета в топ-10 — лише не-фонд з mcap ≥ $300M.
- депег НЕ рахуємо як відрив від $1.00: accrual-токени законно коштують 1.14. Тільки ch24.
- дедуп причин + mute на день (rw_mute_). Кнопки: rw_top_, rw_note_, rw_mute_.
- AI-коментар з тегом MSG_RWA (префікс MSG_ → автоматично інтернет-пошук у grounding).

### Принцип «не вигадувати»
Якщо API/пошта недоступні — модуль молчить або пише «даних немає». Нулі не малюємо.

### Під'єднання
- bot.py: команди /rwa, /підписки, /дати, /дата, /дати_імпорт; обробники sb_*/db_*/rw_*;
  префікси sb_/db_/rw_ додані у верхній диспетчер _route_callback (без нього кнопки мертві).
- intelligent_listener.py: блок «17c-bis» — subs_scan 1800s, subs_due 3600s,
  dates_check 7200s, rwa_moves 1800s.
- monitor.py: блоки 3c/3d/3e у звіт + RWA/підписки/дати в AI-контекст ctx_parts.
Тести: tests_new_trackers.py (20 блоків, ~110 перевірок) — fails 0. Регресія 11 наборів — 0.

### Дата свята з вільного тексту (21.08, після живої перевірки)
Олег написав у чат «01.09. День народження Олі» — текст перехопив планувальник і створив
разову подію в календарі, а в реєстр дат нічого не потрапило (нагадувань за 7/3/1/0 дн. не було б).
Фікс: у dates_book додано looks_like_date_note() (є дата + слово-маркер свята) та
add_from_free_text() (маркер вирізається, тому імʼя = «Олі», а не «День народження»),
плюс added_card() з кнопками 📅 в календар щороку / ✍️ привітання / 🎁 подарунок / ❌ не нагадувати.
У bot.py перехоплення стоїть ДО planner.handle_planner_reply — інакше не спрацює.
Тести: tests_dates_free.py — fails 0.

## ЖИВІ, РІЗНІ ПОВІДОМЛЕННЯ (22.08)

Скарга Олега: «Зроби звіти та сповіщення завжди іншими, щоб відповідали
дійсності, цікавими та інформативними. Щоб не були такі як попередні.
Мають бути живі. Все що надсилають АІ та бот».

Корінь проблеми: анти-повтор існував ЛИШЕ в message_generator.py. Решта ~24
точок AI-тексту (звіт monitor.py, themes_ai, astro_ai, email_ai, rwa_radar,
dates_book, weekly_*, day_mode, deep_analysis) історії не бачили. Реальний
приклад з прода (7cba8af8): «💰 ФІНАНСИ / Зараз немає даних для аналізу».

Фікс — новий variety.py, вшитий у monitor._gem_post (як ai_brain/grounding/
notrunc), тому накриває ВСІ модулі одразу:
  • inject()   — кут подачі (12) + тон (7) + форма (6) з ротацією по тегу і
                 півгодинному слоту; список уже використаних зачинів; банліст
                 15 мертвих фраз; блок FACTS (вимога числа/дати/назви, заборона
                 вигадувати, заборона шаблону «немає даних»);
                 плюс temperature піднімається до 0.85-1.10 (без цього модель
                 повторює формулювання навіть з новим промптом).
  • check()    — minhash по 3-грамах (Jaccard >= 0.50) + однаковий зачин +
                 мертві фрази з банліста.
  • escalate() — зловили повтор → ОДИН перепит із жорсткішою вимогою і
                 temperature на максимум. Це головне: повтор реально
                 відкидається, а не просто «просимо не повторюватись».
  • note()     — відпечаток у storage (variety_log.json, 8 на тег), буфер
                 240 с, щоб звіт не давав 20 комітів.

JSON-промпти не чіпаються взагалі (зламався б парсер) — як у notrunc.

Тести: tests_variety.py (12 блоків, ~55 перевірок) — fails: 0.
Регресія 14 наборів — усі 0. Лінт 3.11 bad: 0.

### Перевірка в проді (22.08)
Деплой e957a16d = SUCCESS. Логи: Traceback 0 / NameError 0 / ImportError 0.
Реальний /звіт через webhook підтвердив:
  [variety] 🎲 варіативність увімкнено для personal_ai
  [variety] 🎲 варіативність увімкнено для astro_ai
  [variety] 🎲 варіативність увімкнено для themes_ai
  [variety] 🎲 варіативність увімкнено для briefing
  [variety] ♻️ action_detect: такий самий зачин — перепитую
  ✅ [storage] SAVED variety_log.json to GitHub
ГОЛОВНЕ: «немає даних для аналізу» у логах звіту — 0 разів (було в themes_ai).
Журнал variety_log.json реально є в origin/data.

БАГ, знайдений цими ж логами, і фікс:
  action_detect — JSON-промпт. inject його (правильно) не чіпав, але check()
  усе одно перевіряв відповідь і бачив однаковий зачин '{"action_type": ...',
  законний для JSON → два марних перепити Gemini + сміття в журналі.
  Фікс: variety.applies(body_bytes) — перевіряємо на повтор ЛИШЕ те, у що
  inject реально втрутився (є MARK). У monitor: check викликається під applies.
  Тест: блок 13 у tests_variety.py.

## ТЕКСТИ: ЦІКАВІШЕ Й ЕФЕКТИВНІШЕ (23.08)

Скарга Олега: «Текст сповіщення звітів нагадування якийсь простий.
Зроби його цікавим, ефективнішим».

variety.py зробив живими AI-тексти, але картки нагадувань складаються НЕ AI,
а f-рядками в модулях — там хвіст був статичний:
  dates_book: «Є час підготуватись без спіху» / «Останній момент купити подарунок»
  subs:       «Якщо не користуєшся — скасувати треба ДО списання»
  bills/deadlines: хвоста не було взагалі, сухий перелік полів

НОВЕ: spice.py — хвіст картки з трьох частин:
  ТЕРМІНОВІСТЬ (реальне число днів, тон під 0/1/3/7+)
  СТАВКА       (що конкретно на кону: пеня, процедура з нуля, гроші)
  КРОК         (одна конкретна дія: «Натисни Текст привітання — 30 секунд»)
  + для підписок money_line(): рахує €/РІК, а не місяць — так рішення очевидніше.
Ротація детермінована (ключ події + поріг + день): 7/3/1/0 дн. дають 4 РІЗНІ
тексти, але повторний виклик у той самий день дає той самий текст (антидубль
не «мутує»). Нічого не вигадуємо: немає суми — немає рядка про гроші.

Підключено: dates_book._head, subs_watcher (due), bills_watcher, deadlines_watcher.
Фікс по дорозі: у bills патч спершу кликав неіснуючу _days_left() → замінено на
готову змінну days_left (інакше хвіст тихо не з'являвся б).

variety.py: додано блок INTEREST для AI-текстів (звітів) — не переказувати дані,
а давати їх зміст; ставка; один конкретний крок із деталями; те, чого Олег сам
не помітив (зв'язок сфер, порівняння з тижнем, аномалія); сильний перший рядок;
обсяг під зміст, без розведеної води.

Тести: tests_spice.py (10 блоків, ~45 перевірок) — fails: 0.
Регресія 15 наборів — усі 0. Лінт 3.11 bad: 0.

### Продовження текстів (23.08)
spice.meal() — нагадування про їжу на зміні (сніданок/обід/вечеря) більше не
статичні: було «• Не забувай про воду 💧» щодня однаково, тепер ротація з
конкретикою (білок зранку, пів літра води на обід, без добавки ввечері, звязок
з цілью 78 кг). Підключено через monitor._spice_meal() — 4 місця (два дублі
блоків зміни), з fallback-текстом якщо spice недоступний.
spice.day_close() — замінив «🌟 Гарного дня!» у astro.py на рядок зі змістом.
Лінт 3.11 bad: 0, AST ok, тести spice/variety/new_trackers/notrunc — 0.

### Стиль і зміст — різні щоразу, можна й коротше (23.08)
Скарга: «Мене цікавить сам стиль зміст щоб був різний можна і коротший, але
цікавим та змістовним».
Що було: промпти жорстко вимагали ДОВГО — 500-650 слів (message_generator),
350-400 слів (рекомендація дня в monitor). Тому кожне повідомлення виходило
однакового розміру і розведене водою.
Зроблено:
1. variety.py — додано дві нові ротації поруч з ANGLES/TONES/SHAPES:
   LENGTHS (6 варіантів обсягу: від «2-3 рядки, тільки суть» і телеграфних
   пунктів до розгорнутого — але тільки коли справді є про що) і
   FOCUS (7 варіантів того, ПРО ЩО говорити: одна сфера / звязок двох сфер /
   динаміка тижня / аномалія / найближчі 24 год / накопичений підсумок /
   чого не зроблено). Плюс окремий рядок ЩІЛЬНІСТЬ: кожен рядок несе цифру,
   висновок або дію — інакше видали.
2. message_generator.py — довгий тригер більше не вимагає 500-650 слів:
   обсяг під зміст (3-5 рядків, якщо одна думка; до 500 слів, якщо є про що),
   структура вільна, порожні блоки не згадуються взагалі.
3. monitor.py — рекомендація дня: замість «350-400 слів» те саме правило
   обсягу під зміст.
Тести: tests_variety 0, tests_spice 0, регресія 0. Лінт 3.11: bad 0.

### Базові правила поведінки AI (промт Олега, 23.08)
Олег дав промт-специфікацію: адаптувати тон/формат, не погоджуватись
автоматично, не вигадувати факти, бути проактивним (думати на 1-2 кроки
вперед, сам пропонувати нагадування/подію/відстеження/автоматизацію), але не
нав'язливо і НЕ завершувати кожне повідомлення фразою «Хочеш, я…».
Вшито централізовано, бо через monitor._gem_post ходять УСІ модулі:
1. variety.CORE — новий блок правил у кожному не-JSON промпті: не погоджуватись
   автоматично й пояснювати чому; без лестощів; думати на 1-2 кроки вперед;
   проактивна КОНКРЕТНА пропозиція (що, коли, з якою умовою); максимум 1-2
   пропозиції; заборона шаблонної кінцівки «Хочеш, я…»; роль під ситуацію
   (експерт / стратег / критичний співрозмовник / просто констатація).
2. variety.check() — новий детект: якщо у хвості тексту (останні 160 символів)
   є «Хочеш, я…» / «Хочеш щоб я…» — це вважається шаблоном і йде перепит.
Тести: tests_variety блок 14 (12 перевірок), fails 0. Лінт 3.11: bad 0.

### Перевірка актуальної інформації — розширено (23.08)
Уточнення: я раніше сказав, що інтернет має лише префікс MSG_ — це було
НЕПРАВИЛЬНО. У GROUND_TAGS уже були personal_ai, themes_ai, daily_rec,
briefing, deep_analysis, crypto_ai, weekly/monthly_coach.
Реально не мали інтернету і додані зараз:
  BRIEFING_GEN (contextual_briefing_engine), REC_GEN (recommendations_engine),
  proactive_actions, health_ai.
Свідомо БЕЗ інтернету лишились: astro_ai (транзити рахуються локально),
email_summary / email_reply (зміст листів локальний, пошук лише сповільнить),
усі JSON-парсери (action_detect, qwatch_parse, email_ai_item) — там grounding
ламає парсер.
Перевірено: grounding.wanted() дає True для 4 нових тегів і False для JSON.

### AI завжди знає день, час і ДЕ Олег (23.08)
Скарга зі скріншотом: о 08:35 бот написав «ти зараз вдома і маєш вільний
ранок», а Олег був на зміні.
КОРІНЬ (знайдено в коді): context.get_status() при today="free" і 7<=h<22
повертає "home" → лейбл «вдома, вільний час». Але today="free" ставилось і
тоді, коли календар просто НЕДОСТУПНИЙ (немає токена / помилка / подія
названа інакше). Плюс message_generator._get_real_status() мав фолбек
«вдома (невизначено з календаря)». Тобто «вдома» було ПРИПУЩЕННЯМ, яке AI
подавав як факт.
Зроблено — новий модуль nowctx.py (єдине джерело правди):
1. Точний час у кожен промпт: дата, день тижня українською, година:хвилина,
   частина дня, номер тижня, Кошице. Плюс правило рахувати «сьогодні/зараз/
   через 2 години» саме від цього часу.
2. Локація має ТРИ стани: work / home / after_night / unknown. Пріоритет
   джерел: ручне слово Олега (свіже, 10 год) -> Google Calendar -> unknown.
   Джерело завжди видно в промпті.
3. При unknown у промпт іде ЗАБОРОНА писати «ти вдома», «вільний ранок»,
   «відпочиваєш», «ти спиш» + вимога або не згадувати локацію, або спитати
   одним рядком «Ти зараз на зміні чи вдома?».
4. При work — заборона радити пробіжку/готування/вільний час, тільки те, що
   реально можливо в перерві або після зміни.
5. Інжект у monitor._gem_post (маркер NOWCTX, ідемпотентно, JSON-промпти не
   чіпаються) -> бачать УСІ модулі.
6. message_generator._get_real_status() більше не каже «вдома» при фолбеку.
7. Нові команди в bot.py: /де (що бот думає про час і місце),
   /робота (=/зміна), /дома. /set_location тепер пише і в nowctx.
Тести: tests_nowctx.py — 8 блоків (~40 перевірок), fails 0. Лінт 3.11 bad 0.

### Скріншот /де підтвердив другу половину бага (23.08, 10:03)
/де відповів: «вдома, вільний день (у календарі зміни немає)», джерело Google
Calendar — а Олег був НА РОБОТІ. Тобто календар доступний, але зміни в ньому
НЕ записані (або названі не «рання»/«нічна»).
Виправлено дві причини:
1. context._SHIFT_KEYWORDS було лише 4 слова («рання», «early», «нічна»,
   «night»). Якщо подія називається «Zmena», «Nočná», «Ranná», «Shift»,
   «Робота», «Minebea», «06:00-18:00» — зміна НЕ розпізнавалась. Розширено до
   ~20 варіантів (укр/слов/англ + часові діапазони).
2. Головне: «у календарі немає події зміни» БІЛЬШЕ НЕ означає «вдома, вільний
   день». Тепер це стан unknown з формулюванням «НЕ ПІДТВЕРДЖЕНО: він міг
   просто не додати зміну» + заборона стверджувати локацію. Впевнене «вдома»
   лишилось тільки для нічних годин (00:00-07:00, 23:00+).
Тести: tests_nowctx 0 падінь.

### Календар = джерело змін + щомісячне нагадування (23.08)
Олег: «Хай читає мій календар. І кожного останнього дня місяця постав
нагадування щоб я надав боту новий графік змін. І перепиши сьогоднішнє
нагадування на ранню зміну — я на ранній зміні.»
1. Календар лишається головним джерелом (свій графік у боті НЕ робимо).
   Розпізнавання подій розширено раніше цього дня до ~20 варіантів назв.
2. nowctx.set_shift(kind) / manual_shift() — ручна зміна на СЬОГОДНІШНЮ дату
   (early/night/free) у storage shift_today.json. Пріоритет ВИЩЕ за календар,
   але діє лише на поточну дату — завтра знову читається календар.
3. Нові команди bot.py: /рання (=/ранкова, /early), /нічна, /вільний.
   /рання також ставить локацію robota, /вільний — doma.
4. Щомісячне нагадування: тред _shift_schedule_reminder_loop у bot.py
   (name=shift_schedule_reminder, перевірка кожні 30 хв). В останній день
   місяця після 10:00 надсилає нагадування внести графік змін на наступний
   місяць + створює подію в Google Calendar на 18:00 (за правилом Олега:
   нагадування = подія в календарі). Дедуп по місяцю в storage
   shift_schedule_asked.json.
Тести: tests_nowctx блок 9 (10 перевірок) — fails 0. Лінт 3.11 bad 0.

## Лист → календар САМ + самоприбирання (23.08)

Олег дав боту повний доступ до календаря: не питати дозволу, а створювати
й повідомляти постфактум.

**mailcal.py** — нове:
- сам читає нові листи (ті самі, що бачить звіт) раз на 45 хв
- AI витягує ЛИШЕ явні дати: рейси, платежі, зустрічі, лікар, дедлайни,
  доставки, скасування. Немає дати в листі → лист пропускається
- подія створюється в Google Calendar ОДРАЗУ
- картка постфактум: що додано + 🗑 прибрати / ✅ все правильно
- захисти: минуле в календар не пишемо, дата далі 400 днів = помилка AI,
  дедуп за uid листа + (дата, назва), пошта впала → нічого не робимо
- команда /листкал — показати створене й прогнати скан вручну

**tidy.py** — нове, раз на добу:
- виконані дедлайни старші 30 днів
- скасовані підписки старші 90 днів; непідтверджені, яких не видно 180 днів
- події з листів, що минули понад 120 днів
- email_deadlines.json: битий рік, нагадане й минуле
- разові дати з реєстру, що минули понад рік (дні народження НЕ чіпаємо)
- звіт «прибрав N записів» зі списком; порожній звіт не надсилається
- команда /прибирання

Підключено: monitor_loop.py (mailcal.run() + tidy.run() у watcher),
bot.py (2 команди + гілка mc_ у _route_callback).
Тести: tests_mailcal.py — 12 блоків, 0 падінь.

## Наглядач над наглядачами (23.08)
watchdog.py — новий модуль. Слідкує за самим ботом і чесно каже, коли осліп.
- LIVE-датчики: пошта, календар, крипто, strava, сховище.
- FRESH: свіжість роботи monitor (≤4 год), mailcal (≤4 год), tidy (≤36 год).
- run(): раз на 55 хв; алерт лише після 2 падінь підряд; пише про ЗМІНИ
  («👁 Я осліп…» / «👁 Знову бачу» з тривалістю); повтор не частіше 12 год.
- digest(): щоденна гарантія о 21:00 — «працює N із M», поіменно сліпі зони,
  плюс що зроблено за день (події з листів, прибрано, дедлайни, підписки).
- Принцип: немає даних = ⚪, нічого не вигадуємо.
- Команда /наглядач (/watchdog, /датчики, /сліпі).
Підключено: monitor_loop.py (run()+digest() у watcher), bot.py (команда).
Тести: tests_watchdog.py — 10 блоків, 0 падінь. Лінт 3.11 bad: 0.

## Кнопки відповіді під кожним сповіщенням + пам'ять реакцій (23.08)
react.py — новий модуль.
- KINDS: власні кнопки під вид сповіщення (лист, рахунок, підписка, дедлайн,
  подія, здоров'я, біг, крипто, звичка, гроші, задача, звіт, загальне).
  detect() визначає вид з тегу/тексту; невідоме → generic (без вигадок).
- ai_kit.send_card: якщо модуль не дав своїх кнопок — кнопки реакції
  прикріплюються автоматично. Тобто вони є під кожним сповіщенням.
- Натискання пишеться в reactions.json назавжди: kind, key, назва, дія, час,
  state (closed / snoozed / open).
- Закриті теми (зробив/оплатив/скасував/не нагадуй/не піду) дублюються в
  dismissed.mute() → тиша одразу в усіх ~50 місцях, що питають is_muted().
- «⏰ Пізніше» = тиша 6 год, потім нагадування повертається.
- react.inject() підмішує реальні реакції в КОЖЕН не-JSON промпт Gemini:
  AI бачить, що вже зроблено, і не тягне закрите в звіти як нове.
  Немає реакцій — немає блоку (нічого не вигадуємо).
- Команда /реакції (/reactions, /кнопки, /закриті).
Тести: tests_react.py — 11 блоків, 0 падінь. Лінт 3.11 bad: 0.

Доповнення: кнопки реакції тепер і під головними звітами
(monitor.send_telegram → _send_telegram_chunk(keyboard=...)), не лише
під картками з ai_kit.send_card.

## Чого Олег НЕ зробив + ініціатива бота (23.08)
openloop.py — новий модуль. Три джерела, усі реальні:
- ЛИСТИ БЕЗ ВІДПОВІДІ: лист старший 3 днів, і в теці «Надіслані» немає ні
  цієї теми, ні листа цьому адресату. Автовідповідачі (noreply) не рахуються.
  Немає дати листа — петлю не вигадуємо.
- ПРОСТРОЧЕНІ НАГАДУВАННЯ: reminders.json, час минув >6 год, закриття немає.
  Щоденні повторювані та древні (>30 дн.) не піднімаємо.
- ОБІЦЯНКИ З ЙОГО Ж ЛИСТІВ: Gemini читає надіслані за 14 днів і бере лише
  явні зобов'язання; дата — тільки якщо вона прямо в листі.
Ініціатива (головна вимога Олега):
- СТВОРЮЄ: обіцянка з датою → подія в Google Calendar сама, повідомляє
  постфактум.
- ЗАПИСУЄ: кожна петля в openloop.json (джерело, назва, час).
- ПИТАЄ: пряме питання з кнопками react.py; відповідь закриває тему назавжди.
Захисти: MAX_ITEMS=3 за прохід, SCAN_GAP_MIN=90, REASK_DAYS=4, повага до
react.is_closed і dismissed.is_muted. IMAP впав → модуль мовчить.
Підключено: monitor_loop.py (watcher), bot.py (/зависло).
Тести: tests_openloop.py — 12 блоків, 0 падінь. Лінт 3.11 bad: 0.

## CORE: правда + формат (23.08, коміт 2836fa396a7)
Додано два правила в `variety.CORE` (іде в КОЖЕН не-JSON промпт через variety.py:400 → monitor._gem_post):
- **НЕ вигадувати факти**: тільки дані цього запиту; немає цифри/дати/суми — казати «немає даних» або не згадувати тему; не округляти здогади до фактів, не малювати нулі замість пропусків, старі дані позначати як старі.
- **Формат свідомо**: дрібниця — 1-3 речення без заголовків; гроші/здоров'я/план/рішення — розгорнуто з висновком.
Решта правил (не погоджуватись автоматично, без лестощів, 1-2 кроки вперед, конкретна проактивність, без «Хочеш, я…», роль під ситуацію) були в CORE уже раніше.
Деплой 6ddd3135-403f-404b-8f18-4ac1e9d2653e SUCCESS, Traceback/NameError/ImportError/SyntaxError = 0.

## money.py — гроші однією лінією (25.08, деплой cd44c27c SUCCESS)
Одна картина замість трьох розкиданих: факти списань із пошти (money_charges.json) + неоплачені рахунки (bills_watcher) + фіксована вага підписок (subs_watcher) + що спишуть за 14 днів.
- ЗАПИСУЄ: Gemini витягує з листів ТІЛЬКИ підтверджені оплати; без достовірної дати/суми/вендора запис не робиться; валюти не змішуються (EUR окремо, решта окремо).
- СТВОРЮЄ: платіж ≥80€ у найближчі 7 днів → подія в календарі, повідомлення постфактум.
- ЗАПИТУЄ: прострочені рахунки; місяць дорожчий за попередній на ≥25% (база ≥40€) — пряме питання «разові витрати чи нова постійна вага».
- Мовчить, коли даних немає. Rate-limit 150 хв, макс 2 картки за прохід, повага до react/dismissed.
Команда /гроші (/money, /витрати, /лінія). Виклик у monitor_loop після openloop. Тести tests_money.py — 0 падінь.

## selfact.py — AI-ІНІЦІАТОР + журнал власних дій (26.08.2026)

Що зроблено:
- `context()` — повна картина для AI: календар 7 днів, свіжі листи з тілами
  (`monitor.get_emails()` віддає dict з `items`, не список!), реєстри
  (bills, subs, money_charges, health, habits, dates, deadlines, openloop,
  ai_notes, reactions), заплановані нагадування, журнал дій за 48 год.
- `decide()` — Gemini JSON: 0-2 дії `{type: note|reminder|event|notify|ask,
  title, text, date, time, why}`. У промпті прямо заборонено вигадувати факти
  й дублювати вже зроблене. Немає причини → `[]`.
- Виконавці: нотатка → `ai_notes.add_note(source="selfact")`;
  нагадування → СПИСОК `reminders.json`, `sent: false`, id `selfact_YYYYMMDDHHMM`
  (дата через `K.valid_future_date` — минула дата відкидається);
  подія → `K.calendar_event(datetime)`; notify/ask → `K.send_card` з кнопками react.
- Після кожної створеної речі Олегу летить «🤖 Зробив сам: …» + рядок «Чому».
- `journal(kind, what, detail, module)` → `bot_actions.json` (400 записів).
  ПУБЛІЧНИЙ хелпер — інші модулі можуть писати свої дії туди ж.
- `digest()` — міні-звіт 21:30 «Що я зробив сам за сьогодні», групування
  подія/нагадування/нотатка/питання/сповіщення + підрахунок за реєстрами
  (mailcal, money_charges, money_events, reactions). Без дій — каже прямо.
- `run(force)` — rate-limit 90 хв (`selfact_scan.json`), макс 2 дії,
  дедуп `selfact_dedup.json` (5 днів), повага до `react.is_closed` і
  `dismissed.is_muted`.
- Інтеграція: `monitor_loop.py` у watcher-циклі після `healthtrend`;
  команди `/сам` (`/ініціатива`, `/selfact`) і `/зроблено` (`/дії`) у `bot.py`.

Перевірено: `tests_selfact.py` — 13 блоків, 0 падінь. Деплой c6ed3bf5 SUCCESS,
у проді 0 помилок, живий прохід: `[selfact] виконано дій: 2` (сповіщення про
невдалі платежі Runable/Railway + нагадування оновити платіжні дані),
`bot_actions.json` створений у гілці data.

## qwsync.py — АВТОСИНК здоров'я з QWatch Pro (26.08.2026)

Факт: QWatch Pro (com.qcwireless.qcwatch) НЕ має публічного API й експорту.
Єдиний офіційний вихід даних — на iOS синхронізує кроки/калорії/дистанцію/
пульс/сон у Apple Health. Тому ланцюжок:
  QWatch Pro → Apple Health → Apple Shortcuts (автоматизація за часом)
  → POST /qw → qwatch_data.json.

Чому qwatch_data.json: `storage.load_health()` мерджить його ПОВЕРХ health.json,
тому дані одразу бачать усі звіти, AI-контексти, healthtrend, графіки, selfact.

- `qwsync.normalize()` — приймає «як завгодно» названі поля (Shortcuts віддає
  рядки): steps/кроки, sleep/sleep_min/sleep_h, hr/heart_rate/пульс, calories,
  distance, weight, hrv, spo2, health_score, body_battery, stress.
  Розбирає '4 989' → 4989, '09h 08min' → 548 хв, '83,4' → 83.4.
  Відсіює сміття: кроки 0..100000, пульс 25..230, сон 0..1080 хв, вага 35..250.
- `qwsync.save()` — мердж у наявний запис дня (нічого не затирає), сповіщення в
  Telegram + `selfact.journal`.
- Маршрут у `health_webhook.py`: `POST /qw` (також /qwatch, /health),
  БЕЗ часового вікна (у Widgets-handler вікно 18:45-19:15 — не годиться).
  Захист: заголовок `X-QW-Secret` або поле `secret` у JSON.
  Змінна `QW_SECRET` на Railway = oleh-qwatch-2026 (фолбек —
  TELEGRAM_WEBHOOK_SECRET).
- health_webhook.py — ЄДИНИЙ HTTP-сервер бота в проді (Railway PORT), уже
  обробляє /telegram, /upload (ZIP HAE), POST / (Healthy Widgets).

Перевірено живим POST у прод: HTTP 200,
{"ok":true,"fields":["steps","hr_avg","weight_kg","sleep_total_min"]},
у гілці data з'явилось 2026-08-26: steps 4989, hr_avg 71, weight_kg 83.4,
sleep_total_min 548. Traceback 0.

## httpreq.py — фікс щогодинних звітів (26.08.2026)
ПРИЧИНА: у рантаймі Railway відсутній модуль `requests`. main() у monitor.py
падав на `import requests as _req_send` (рядок ~11113) — уже ПІСЛЯ того, як
забрав lock. Тому в data/monitor_main_sent.json lock_slot оновлювався щогодини,
а sent_slot застряг на 2026-08-25T11:00. Жоден звіт не доходив з 25.08.
ФІКС: httpreq.py — urllib-шим із API `requests` (post/get, multipart для фото,
Response.status_code/.text/.json()). У monitor.py три місця імпорту
(top-level _requests, _send_photo_bytes _req_pb, main() _req_send) тепер
падають у фолбек `import httpreq`.
ПЕРЕВІРЕНО: прод-лог — "[send] requests відсутній — використовую httpreq
(urllib)", 6× "send_telegram single chunk returned True", "=== Report sent ===",
Traceback 0.

## healthai.py — здоров'я під AI (26.08.2026)
ПРИЧИНА ВСІХ ТРЬОХ ПРОБЛЕМ (астро, графіки, Strava): Railway перейшов на білдер
railpack — залежності ставляться у /app/.venv/lib/python3.11/site-packages, а
процес піднімався системним python (/mise/installs/...), де НЕМА нічого.
Лог: "[diag] НЕМА: requests, kerykeion, matplotlib, numpy, yfinance, ...".
ФІКС: _ensure_venv() у monitor_loop.py додає site-packages венва в sys.path.
Після фіксу: "[diag] є: requests, kerykeion, matplotlib, ... | НЕМА: —".
Плюс httpreq-фолбек у 10 модулях (strava, portfolio, traffic, report*, meds, bot).

healthai.py:
- capture(text) — зберігає ВСЕ, що надсилає Олег: сирий текст у health_journal.json
  + розібрані числа через qwsync у qwatch_data.json. Хук у bot.py на кожне
  повідомлення зі словами вага/сон/кроки/пульс/hrv/ккал/spo2/тиск.
- analytics(days) — вага/сон/кроки/пульс/HRV/калорії: last, avg, avg7, min, max,
  тренд (перша vs друга половина періоду), streak, пропущені дні, stale_hours,
  прогрес до 78 кг, км бігу за тиждень.
- ai_analysis / ai_recommendations — Gemini строго на цих числах.
- coach_report() 08:15, tracker_report() 21:15, weekly_report() нд 19:30.
- initiative() кожні 30 хв: weight_up / sleep_low / steps_low / hr_high / stale
  (дані не оновлювались 36 год), дедуп 12 год на привід, повага до quiet+dismissed.
- tick() — викликається з monitor_loop щохвилини.
- Команди: /здоров'я /коуч /трекер. Кнопки hai_stats/hai_reco/hai_ok/hai_mute.
ТЕСТИ: tests_healthai.py — 13 блоків, 0 падінь.

## 03.09.2026 — «завжди питати» + форсайт (в роботі)
- askme: набір "remind" (🔔 Так, нагадай / 🕒 Інший час / 🚫 Не треба) + _do_save() —
  реальний запис у reminders.json ЛИШЕ після натискання, + подія в календар (allow_once).
- calgate: worth_asking тепер ПИТАЄ про все, крім реклами/трекерів/рутини (_JUNK).
  Новий gate_write(kind=reminder|note|shopping|task) — жодного запису без «так».
- selfact._do_reminder → через calgate.gate_write.
- foresight.py (НОВИЙ): дивиться вперед — поїздки (місто+погода open-meteo+речі+
  що відвідати+дії до вильоту), блок нічних змін + незакриті справи, листи
  (важливі спершу, реклама одним рядком). tick() раз на 150 хв, дедуп у
  foresight_state.json. Усі записи — через askme.ask(kind="remind").
- Тест /tmp/t_foresight.py — 29/29 ok. Hook у monitor_loop (після selfact), команда
  /вперед /форсайт /foresight (звіт + примусовий tick у потоці).
